@model IEnumerable<TechSavvy.Models.PurchaseRequest_Details>

<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Suppliers</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
</head>
<br />
<legend class="bg bg-info text-center rounded-pill">Purchase Request Details</legend>
<hr />
<body>
    <div class="container p-3">

        <div class="form-group row pt-4">
            <div class="col-6 text-right">
                <div class="col-6 text-right">
                    <a asp-action="Create" asp-controller="PurchaseRequest" class="btn btn-primary">Create New Purchase Request</a>
                </div>
            </div>
        </div><br />

        <br />


        <table class="table">
            <thead>
                <tr class="bg-primary">
                    
                    <th>
                        @Html.DisplayNameFor(model => model.Purchase_Request.requestDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Product.name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.quantity)
                    </th>

                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    ViewBag.Message = "You currently do not have any recods";
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            @* <td>
                    @Html.DisplayFor(modelItem => item.Products.description)
                    </td>
                    <td>
                    @Html.DisplayFor(modelItem => item.description)
                    </td>
                    <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                    </td>*@
                            <td>
                                @Html.DisplayFor(modelItem => item.Purchase_Request.requestDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Product.name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.quantity)
                            </td>

                            @*<td>


                                <a asp-action="Edit" class=" btn btn-warning " asp-route-id="@item.Id">Edit</a>
                                <a asp-action="Delete" class=" btn btn-danger " asp-route-id="@item.Id">Delete</a>

                            </td>*@
                        </tr>
                    }


                }

            </tbody>
        </table>

        <div class="form-group">
            <div class="form-control">

                <a class=" nav-link" href="javascript:window.print()" href="javascript:window.print()">
                    <button class=" btn btn-primary">print</button>
                </a>
                <a class="btn btn-primary" asp-controller="" role="button">Back</a>

            </div>


        </div>


    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
</body>
</html>